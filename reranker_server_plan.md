# 🧠 Dedicated Re-Ranker Microservice Plan

## 1. الفكرة المعمارية (Architectural Vision)
بناء السيرفر الرئيسي (Next.js) ليكون مسؤولاً عن تقديم الواجهات والـ APIs الخفيفة، بينما نقوم ببناء **سيرفر مصغر مستقل (Microservice)** مسؤوليته الوحيدة في الحياة هي تشغيل موديل الـ Re-Ranker.

### لماذا هذه أفضل ممارسة (Best Practice)؟
1. **حماية الرامات (Memory Isolation):** موديلات الذكاء تحتاج إلى Caching ورامات عالية. وضعها داخل Next.js سيجعل الموقع بالكامل بطيئاً. فصلها يضمن بقاء الموقع سريعاً جداً.
2. **استقلالية التوسع (Independent Scaling):** إذا زاد الضغط على البحث، يمكنك إضافة سيرفرات للـ Re-Ranker فقط دون الحاجة لاستنساخ سيرفرات الموقع بالكامل.
3. **تعدد اللغات (Polyglot Friendly):** يمكننا برمجته بـ Node.js أو Python متى أردنا.

---

## 2. التقنيات المقترحة (Tech Stack)
* **الإطار (Framework):** `Express.js` أو `Fastify` (لسرعته العالية جداً في معالجة طلبات الـ HTTP).
* **مكتبة الذكاء الاصطناعي:** `@xenova/transformers` (لغة JavaScript) لتشغيل الموديل محلياً.
* **الموديل المقترح:** `Xenova/bge-reranker-base` (سريع وممتاز جداً في اللغة الإنجليزية والعربية معاً).
* **إدارة التشغيل:** `PM2` أو `Docker` لضمان عمل السيرفر في الخلفية وإعادة تشغيله تلقائياً إذا تعطل.

---

## 3. تصميم الـ API (API Design)

سيكون للسيرفر نقطة دخول (Endpoint) واحدة فقط:
```http
POST /api/rerank
Content-Type: application/json

{
  "query": "هل نزلت أسهم شركة أبل؟",
  "documents": [
    { "id": "1", "text": "Apple released a new phone." },
    { "id": "2", "text": "Tech company stock prices dropped yesterday, including Apple." }
  ],
  "top_k": 1
}
```

**الرد المتوقع (Response):**
```json
{
  "results": [
    { "id": "2", "score": 0.98 },
    { "id": "1", "score": 0.12 }
  ]
}
```

---

## 4. خطوات التنفيذ العملي (Implementation Steps)

### الخطوة الأولى: إنشاء مشروع السيرفر المستقل (بجانب مشروعك الحالي)
سنقوم بإنشاء مجلد جديد مثلاً `rag-reranker-service`، وتهيئته `npm init -y` وتسطيب المكتبات الأساسية:
```bash
npm install express cors body-parser @xenova/transformers
```

### الخطوة الثانية: كتابة كود السيرفر (`server.js`)
استدعاء الموديل مرة واحدة عند تشغيل السيرفر (Cold Start) ليكون جاهزاً في الذاكرة (RAM)، ثم إنشاء الـ API لاستقبال النصوص وتقييمها وإرجاعها مرتبة.

### الخطوة الثالثة: ربط السيرفر الرئيسي (Next.js) بالخدمة الجديدة
في ملف [src/app/api/v1/chat/route.ts](file:///g:/momen%20fci%204/rag-platform-V1/src/app/api/v1/chat/route.ts)، بعد جلب الـ 15 مستنداً من Qdrant، سنقوم بإرسالهم عبر `fetch` إلى سيرفرنا الجديد (مثلاً `http://localhost:4000/api/rerank`). سيرفر الـ Re-Ranker سيرسل أفضل 5 مستندات، ونمررهم إلى الـ LLM مباشرة.

### الخطوة الرابعة: النشر والتشغيل (Deployment)
تجهيز ملف `ecosystem.config.js` لتشغيله باستخدام `pm2` ليظل يعمل كخدمة دائمة (Background Service).

---

## 5. مزايا هذه الخطة 🌟
* لن يتم العبث بكود مشروعك الرئيسي بشكل كبير.
* الـ API الخاص بـ Next.js الخاص بك لن يستهلك أكثر من 100 ميجابايت من الرام.
* السيرفر الجديد يمكن تشغيله على بورت مختلف `4000` أو حتى استضافته على جهاز خادم (VPS) آخر أقوى إذا احتجت لذلك مستقبلاً.



ما الذي تم إنجازه الآن؟

📂 قمت بإنشاء مجلد جديد باسم reranker-service داخل مشروعك الأساسي.
⚙️ تم إنشاء السيرفر باستخدام Express و Transformers.js لتحميل موديل Xenova/bge-reranker-base.
📦 قمت بتشغيل أمر التسطيب (npm install) في الخلفية لتحميل المكتبات المطلوبة.
🔌 قمت بتعديل نظام الـ RAG الرئيسي لديك (src/app/api/v1/chat/route.ts) ليقوم بإرسال أفضل 15 نتيجة إلى هذا السيرفر الجديد عبر http://localhost:4000/api/rerank، وإذا كان السيرفر متوقفاً، سيعود تلقائياً للنظام القديم بدون أن تنهار المنصة (Graceful Fallback)!
كيفية التشغيل والتجربة: لتجربة هذا التحديث الثوري، قم بفتح منفذ أوامر جديد (New Terminal)، ثم نفذ التالي:

bash
cd "reranker-service"
npm run dev
سيبدأ السيرفر في تحميل الموديل الذكي لمرة واحدة فقط (لذا انتظر الثواني الأولى حتى يطبع ✅ Re-Ranker model loaded successfully).

بمجرد عمله، اذهب للمنصة وجرب طرح سؤال! ستلاحظ أن الأجوبة أصبحت أكثر دقة بكثير واستبعدت تماماً أي مستندات غير متعلقة 100% بسؤالك. ما رأيك؟

